{
  "name": "mirror-life",
  "version": "0.4.0",
  "description": "镜像人生 / MirrorLife — 个人叙事实验型社会模拟游戏",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "node --check public/engine.js && node --check public/narrative.js && node --check public/causal-graph.js && node --check public/game.js && node --check public/storage.js && node --check public/memory-hub.js && node --check public/story-engine.js && node --check server/memory-proxy.mjs",
    "check:copy": "node scripts/review-copy.mjs",
    "test": "npm run check",
    "test:evolution": "node scripts/verify-evolution.mjs",
    "generate:interior-glb": "node scripts/generate-interior-glbs.mjs",
    "generate:interior-props-image2": "node scripts/generate-interior-props-image2.mjs",
    "memory-proxy": "node server/memory-proxy.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "keywords": [
    "virtual-society",
    "simulation",
    "game",
    "ai"
  ],
  "license": "MIT",
  "author": "",
  "repository": {},
  "devDependencies": {
    "vite": "^8.0.13"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "three": "^0.185.1"
  }
}
