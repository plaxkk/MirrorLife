# 被视觉检查否决的中间候选

`overlapping-trim.png` 显示新侧墙与旧 flowing shell、冠条及底座重叠。
`automated-pass-not-art-acceptance.json` 是该中间候选真实通过的自动检查结果，但**不是最终美术放行**。

修正后完整候选重新执行验收；当前有效结果位于 `../../acceptance.json`。不得把两个不同指纹的采样拼接成一次验收。
