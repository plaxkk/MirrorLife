# MirrorLife 项目规则

## 会话规则

- 项目初期阶段，非重大功能修改（bugfix、UI微调、小功能迭代、代码清理等），不要请示用户，自己全权负责执行。
- 只有涉及重大功能方向变更、架构重构、破坏性操作时才需要确认。
- 后续本项目完成代码修改并通过必要检查后，默认自动提交并 push 到远端；除非用户明确要求只改本地或存在安全/凭据/破坏性风险。

## 浏览器验证规则

- 启用真实浏览器窗口进行验证时，任何时刻同时新开的浏览器窗口不得超过 5 个。
- 优先复用已经打开的浏览器窗口或标签页；验证完成后及时关闭不再使用的窗口。
